<Window
    x:Class="BGH_Kompakt.Views.Windows.PreviewEMail"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:BGH_Kompakt.Views.Windows"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="EMail-Vorschau"
    Width="800"
    Height="550"
    WindowStartupLocation="CenterOwner"
    WindowStyle="SingleBorderWindow"
    mc:Ignorable="d">
    <Window.Resources>
        <Style
            x:Key="Text_Display"
            BasedOn="{StaticResource textblock_base}"
            TargetType="TextBlock">
            <Setter Property="TextWrapping" Value="Wrap" />
            <Setter Property="Margin" Value="0,5" />
        </Style>
        <Style
            x:Key="Label_Display"
            BasedOn="{StaticResource Label_Base}"
            TargetType="Label">
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Margin" Value="10,5" />
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Label
                Grid.Row="0"
                Grid.Column="0"
                Content="Adressat:"
                Style="{StaticResource Label_Display}" />
            <TextBlock
                x:Name="Text_EMailTo"
                Grid.Row="0"
                Grid.Column="1"
                Style="{StaticResource Text_Display}" />
            <Label
                Grid.Row="1"
                Grid.Column="0"
                Content="Adressat BCC:"
                Style="{StaticResource Label_Display}" />
            <TextBlock
                x:Name="Text_EMailToBCC"
                Grid.Row="1"
                Grid.Column="1"
                Style="{StaticResource Text_Display}" />
            <Label
                Grid.Row="2"
                Grid.Column="0"
                Content="Betreff:"
                Style="{StaticResource Label_Display}" />
            <TextBlock
                x:Name="Text_Subject"
                Grid.Row="2"
                Grid.Column="1"
                Style="{StaticResource Text_Display}" />
            <Label
                Grid.Row="3"
                Grid.Column="0"
                Content="Textkörper:"
                Style="{StaticResource Label_Display}" />
            <TextBlock
                x:Name="Text_Body"
                Grid.Row="3"
                Grid.Column="1"
                Style="{StaticResource Text_Display}" />
        </Grid>
        <Button
            x:Name="Btn_close"
            Grid.Row="1"
            Margin="0,0,15,0"
            HorizontalAlignment="Right"
            Click="Btn_close_Click"
            Content="Schließen"
            Style="{StaticResource Button_Master}" />
    </Grid>
</Window>
