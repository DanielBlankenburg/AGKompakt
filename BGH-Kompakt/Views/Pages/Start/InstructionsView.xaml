<UserControl
    x:Class="BGH_Kompakt.Views.Pages.Start.InstructionsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:BGH_Kompakt.Views.Pages.Start"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <UserControl.Resources>
        <Style
            x:Key="Button_Anleitungen"
            BasedOn="{StaticResource Button_Master}"
            TargetType="Button">
            <Setter Property="Margin" Value="0,0,0,10" />
        </Style>
    </UserControl.Resources>
    <Grid Background="{StaticResource MaterialDesignPaper}">
        <Grid.RowDefinitions>
            <RowDefinition Height="60" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock
            Grid.Row="0"
            Grid.Column="0"
            Grid.ColumnSpan="2"
            Margin="0,10,0,0"
            Style="{StaticResource Style_Textblock_Titel}"
            Text="Anleitungen" />
        <StackPanel Grid.Row="1">
            <Button
                x:Name="Allgemein"
                Click="Button_Click"
                Content="Geschäftsstellenbereich"
                Style="{StaticResource Button_Anleitungen}" />
            <Button
                x:Name="Montagspost"
                Click="Button_Click"
                Content="Montagspost"
                Style="{StaticResource Button_Anleitungen}" />
            <Button
                x:Name="Nebentätigkeiten"
                Click="Button_Click"
                Content="Nebentätigkeiten"
                Style="{StaticResource Button_Anleitungen}" />
        </StackPanel>
    </Grid>
</UserControl>
