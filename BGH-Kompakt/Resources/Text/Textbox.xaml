<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:MD="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:MW="http://metro.mahapps.com/winfx/xaml/controls">

    <!--#region Textbox-->
    <Style
        x:Key="Style_Textbox_Spruchgruppen"
        BasedOn="{StaticResource MahApps.Styles.TextBox}"
        TargetType="{x:Type TextBox}">
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Margin" Value="15,0,0,0" />
        <Setter Property="FontFamily" Value="{StaticResource MaterialDesignFont}" />
        <Setter Property="Height" Value="30" />
        <Setter Property="Width" Value="50" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
    </Style>

    <Style x:Key="Style_Textbox_PersonChange" TargetType="{x:Type TextBox}">
        <Setter Property="FontFamily" Value="{StaticResource MaterialDesignFont}" />
        <Setter Property="Margin" Value="0,10,0,0" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
    </Style>

    <Style
        x:Key="Style_Textbox_Userlogin"
        BasedOn="{StaticResource MahApps.Styles.TextBox}"
        TargetType="{x:Type TextBox}">
        <Setter Property="FontFamily" Value="{StaticResource MaterialDesignFont}" />
    </Style>
    <Style x:Key="Textbox_Textarea" TargetType="{x:Type TextBox}">
        <Setter Property="FontFamily" Value="{DynamicResource MaterialDesignFont}" />
        <Setter Property="FontSize" Value="{DynamicResource BaseFontSize}" />
        <Setter Property="TextWrapping" Value="WrapWithOverflow" />
    </Style>
    <Style
        x:Key="Textbox_Base"
        BasedOn="{StaticResource MaterialDesignFloatingHintTextBox}"
        TargetType="TextBox">
        <Setter Property="FontFamily" Value="{DynamicResource MaterialDesignFont}" />
        <Setter Property="FontSize" Value="{DynamicResource BaseFontSize}" />
        <Setter Property="Cursor" Value="Arrow" />
        <!--<Setter Property="FontWeight" Value="Bold" />-->
        <!--<Setter Property="BorderBrush" Value="{StaticResource AppColorCyan}" />-->
        <Setter Property="MD:HintAssist.Foreground" Value="DarkGray" />
        <Setter Property="MD:HintAssist.FloatingScale" Value=".8" />
        <Setter Property="MD:HintAssist.FloatingOffset" Value="1,-18" />
        <Setter Property="MD:HintAssist.HintOpacity" Value="0.5" />
    </Style>

    <Style x:Key="Test_Smarthint" TargetType="{x:Type MD:SmartHint}">
        <Setter Property="FontSize" Value="8" />
    </Style>

    <!--#endregion-->



</ResourceDictionary>