<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:MD="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:MW="http://metro.mahapps.com/winfx/xaml/controls">

    <!--#region Combobox-->

    <Style
        x:Key="Combobox_Base"
        BasedOn="{StaticResource MaterialDesignComboBox}"
        TargetType="{x:Type ComboBox}">
        <Setter Property="Control.FontFamily" Value="{DynamicResource MaterialDesignFont}" />
        <Setter Property="Control.FontSize" Value="{DynamicResource BaseFontSize}" />
        <Setter Property="MD:HintAssist.Foreground" Value="DarkGray" />
        <Setter Property="MD:HintAssist.HintOpacity" Value=".5" />
        <Setter Property="MD:HintAssist.IsFloating" Value="True" />
        <Setter Property="MD:HintAssist.FloatingScale" Value=".8" />
        <Setter Property="MD:HintAssist.FloatingOffset" Value="1,-18" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Cursor" Value="Hand" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="Style_Combobox" TargetType="{x:Type ComboBox}">
        <Setter Property="Control.FontFamily" Value="{StaticResource MaterialDesignFont}" />
        <Setter Property="Control.FontSize" Value="16" />
    </Style>

    <Style
        x:Key="Combobox_Eingabefenster"
        BasedOn="{StaticResource MahApps.Styles.ComboBox}"
        TargetType="{x:Type ComboBox}">
        <Setter Property="Margin" Value="0" />
        <Setter Property="HorizontalAlignment" Value="Center" />
    </Style>

    <Style
        x:Key="Combobox_WindowCommand"
        BasedOn="{StaticResource MaterialDesignComboBox}"
        TargetType="{x:Type ComboBox}">
        <Setter Property="FontFamily" Value="{StaticResource MaterialDesignFont}" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Margin" Value="5,0,0,0" />
    </Style>



    <Style x:Key="Style_combobox_Eingabefenster" TargetType="{x:Type ComboBox}">
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Margin" Value="10,0" />
        <Setter Property="FontFamily" Value="{StaticResource MaterialDesignFont}" />
    </Style>


    <!--#endregion-->


</ResourceDictionary>